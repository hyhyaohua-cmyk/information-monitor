# 信息监控工具维护说明

更新时间：2026-08-18

这份文件用于在原对话丢失、换电脑或重新打开 Codex 后继续维护项目。程序代码以本地目录和 GitHub 仓库为准，不依赖原对话才能运行。

## 重要入口

- GitHub 仓库：<https://github.com/hyhyaohua-cmyk/information-monitor>
- 手动刷新：<https://github.com/hyhyaohua-cmyk/information-monitor/actions/workflows/news-monitor.yml>
- 在线报告：<https://hyhyaohua-cmyk.github.io/information-monitor/>
- 本地项目：`/Users/yaohuahuang/NewsMonitor`
- 本地启动器：`/Applications/新闻监控工具.app`
- 本地停止器：`/Applications/停止新闻监控.app`

## 当前功能

工具分为“新闻、智库、央行”三类，可分别手动刷新。当前配置包含44个新闻来源、36个智库来源和13个央行来源；完整名单以 `app.py` 中的配置为准。

每个“网站 × 采集通道”分别保存基线。通道首次成功读取时只记录现有链接，不报告新增；超时或失败不会建立基线，恢复后的首次成功仍只建立该通道基线。从第二次成功读取开始，未见过且符合规则的链接才进入报告。

主要处理规则：

- 同一链接跨首页、RSS/Atom 和 sitemap 去重。
- 只保留英语内容，并生成中文标题；缺失标题时优先从网址路径提取。
- 过滤链接中可识别的历史日期，同时兼顾北京时间和美东时间当天。
- 过滤雅虎财经、BigGo Finance 等行情详情页和其他已知无意义链接。
- RSS/Atom 有发布时间时，过滤不在北京时间或美东时间当天联合窗口内的旧内容。
- 报告保留3天；删除历史后，链接指纹继续保留，避免再次误报。
- 中文标题、原始网址和来源名称均可点击，并在新标签页打开。
- 报告支持类别、刷新时间、关键词和多网站筛选。

## 日常使用

### GitHub 版

打开“手动刷新”链接，点击 **Run workflow**，选择“新闻、智库或央行”。刷新完成后打开“在线报告”。电脑可以关机，运行不消耗 GPT、ChatGPT 或 Codex Token。

GitHub 会保存最新的基线和去重状态。建议至少每90天成功运行一次，避免状态备份因保留期限到期而被删除。

### 本地版

从“应用程序”打开“新闻监控工具”，浏览器会进入本地页面；需要停止后台服务时打开“停止新闻监控”。本地网页与数据库只保存在这台电脑。

## 两个版本的数据关系

本地版与 GitHub 版可以使用相同代码，但数据库完全独立：

- 本地基线、去重状态和刷新历史保存在本地 `news_monitor.sqlite3`。
- GitHub 基线、去重状态和刷新历史保存在 Actions artifact。
- 修改代码时可以同时更新两版，但两边的历史和基线不会自动合并或同步。

本地数据库、日志、生成报告、macOS `.app` 启动器和备份均受 `.gitignore` 保护，不上传 GitHub。

## 当前状态

- 2026-08-17 已分别完成“新闻、智库、央行”的首次 GitHub 基线。
- GitHub Pages 已启用，工作流使用支持 Node.js 24 的官方 Actions 组件。
- 本地启动器已移入“应用程序”。旧启动器备份位于 `/Users/yaohuahuang/NewsMonitor/旧版启动器备份-20260817`。
- GitHub 仓库为公开仓库，因此程序代码和在线报告公开可见。

## 以后如何修改

在新的 Codex 对话中发送下面这句话，并附上具体需求：

> 请维护信息监控工具，本地目录是 `/Users/yaohuahuang/NewsMonitor`，GitHub 仓库是 `hyhyaohua-cmyk/information-monitor`。请同时更新本地版和 GitHub 版，保留现有数据库、基线、刷新历史和用户改动。

修改时应遵循：

1. 先检查 Git 状态，不覆盖无关改动。
2. 修改 `app.py`、`index.html`、`github_actions.py` 或工作流后，运行 `test_app.py`。
3. 不提交 `news_monitor.sqlite3`、日志、`public/` 或 `.app`。
4. 更新 GitHub 后，运行一个类别重新生成在线报告；普通代码更新不得清空 GitHub 状态 artifact。
5. 新增或删除网站时，同步检查类别、过滤规则、标题处理和测试。

## 关键文件

- `app.py`：网站清单、采集、过滤、翻译、数据库和本地服务。
- `index.html`：本地操作页面。
- `github_actions.py`：GitHub 刷新入口和静态报告生成。
- `.github/workflows/news-monitor.yml`：GitHub Actions 工作流。
- `test_app.py`：回归测试。
- `README.md`：公开使用说明。

## 已知边界

部分网站会登录验证、地区限制、JavaScript 校验、限流或临时超时。失败通道不会破坏其他通道，也不会建立错误基线；该通道恢复后的首次成功只建立自己的基线。某些文章本身可能需要订阅或登录，工具只能保证链接可点击，不能绕过媒体的访问限制。
